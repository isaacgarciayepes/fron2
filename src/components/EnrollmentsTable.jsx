function EnrollmentsTable({ enrollments, students, courses, onEdit, onDelete }) {
  if (!enrollments?.length) return <p className="text-gray-500 p-4">Todavía no hay inscripciones registradas.</p>;
  const studentName = (id) => { const s = students.find((item) => item.id === id); return s ? `${s.firstName} ${s.lastName}` : "Estudiante no encontrado"; };
  const courseName = (id) => { const c = courses.find((item) => item.id === id); return c ? `${c.code} — ${c.name}` : "Curso no encontrado"; };
  return <div className="overflow-x-auto"><table className="w-full text-left border-collapse bg-white rounded-xl shadow overflow-hidden"><thead><tr className="border-b bg-gray-50"><th className="p-4">Estudiante</th><th className="p-4">Curso</th><th className="p-4">Fecha</th><th className="p-4">Estado</th><th className="p-4">Acciones</th></tr></thead><tbody>{enrollments.map((e) => <tr key={e.id} className="border-t"><td className="p-4">{studentName(e.studentId)}</td><td className="p-4">{courseName(e.courseId)}</td><td className="p-4">{e.enrollmentDate}</td><td className="p-4"><span className={`px-2 py-1 rounded-full text-xs font-medium ${e.status === "ACTIVE" ? "bg-green-100 text-green-700" : "bg-gray-200 text-gray-600"}`}>{e.status === "ACTIVE" ? "Activa" : "Cancelada"}</span></td><td className="p-4"><div className="flex gap-3"><button onClick={() => onEdit?.(e)} className="text-blue-600 hover:underline text-sm font-medium">Editar</button><button onClick={() => onDelete?.(e)} className="text-red-600 hover:underline text-sm font-medium">Eliminar</button></div></td></tr>)}</tbody></table></div>;
}
export default EnrollmentsTable;
